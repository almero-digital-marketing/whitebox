# Copy to .env.test and fill in. .env.test is gitignored — never commit real values.
# Tests spin up a throwaway Neon branch per run.
NEON_API_KEY=napi_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
NEON_PROJECT_ID=your-neon-project-id
